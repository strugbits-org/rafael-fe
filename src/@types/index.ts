interface annoucements {
  title: string;
  description: string;
  profilePic: string;
  userName: string;
}

interface bestEmployee {
  name: string;
  image: string;
}

export type { annoucements, bestEmployee };
